(Test time operations and timers)

(Get current time)
Get current time into now.
Print "Current timestamp: ".
Print now's unix.
Print "Current hour: ".
Print now's hour.
Print "Current minute: ".
Print now's minute.
Print "Current second: ".
Print now's second.
Print "Current day: ".
Print now's day.
Print "Current month: ".
Print now's month.
Print "Current year: ".
Print now's year.

(Inline time access)
Print "Inline hour: ".
Print current time's hour.
Print "Inline minute: ".
Print current time's minute.

(Timer operations)
Create a timer called "test_timer".
Start the "test timer".
Wait 100 milliseconds.
Stop the "test timer".
Print "Timer duration in ms: ".
Print "test timer"'s duration.
Print "Timer duration in seconds: ".
Print "test timer"'s duration in seconds.

(Multiple timers)
Create a timer called "fast_timer".
Create a timer called "slow_timer".
Start the "fast timer".
Start the "slow timer".
Wait 50 milliseconds.
Stop the "fast timer".
Wait 100 milliseconds.
Stop the "slow timer".
Print "Fast timer: ".
Print "fast timer"'s duration.
Print "Slow timer: ".
Print "slow timer"'s duration.

(Timer with operations)
Create a timer called "calc_timer".
Start the "calc timer".
a number called "result" is 0.
For each i from 1 to 1000,
    Set result to result add i.
Stop the "calc timer".
Print "Calculation result: ".
Print result.
Print "Calculation time: ".
Print "calc timer"'s duration.

(Time arithmetic)
Get current time into start_time.
Print "Start time: ".
Print start_time's unix.
Wait 1 second.
Get current time into end_time.
Print "End time: ".
Print end_time's unix.
Print "Difference: ".
Print end_time's unix subtract start_time's unix.
