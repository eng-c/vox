print "=== SECURITY TEST: Buffer Overflow Attempts ===".

print "Test 1: Reading a huge file into buffer".
a buffer called "huge".

print "Creating a 1MB test file...".
open a file for writing called out at "./huge_test.txt".
a number called "i" is 0.

While i is less than 10000,
    Write "Line of text for testing buffer growth capabilities in the dynamic buffer system!" to out,
    Write a newline to out,
    Increment i.

Close the out.

print "Reading 1MB+ file into dynamic buffer...".
open a file for reading called src at "./huge_test.txt".
Read from src into huge.
Close the src.

print "Success! Buffer grew dynamically.".
print "Cleaning up...".
Delete the file "./huge_test.txt".

print "Test 2: Multiple buffers".
a buffer called "buf1".
a buffer called "buf2".
a buffer called "buf3".

print "All buffers allocated safely.".

print "=== ALL OVERFLOW TESTS PASSED ===".
